import React from "react";

const Loader: React.FC = () => {
  return <section className="absolute top-0 left-0 w-screen h-screen bg-black z-20 flex items-center justify-center">
  </section>;
};

export default Loader;
